<p id="notice"><%= notice %></p>

<p>
  <strong>Word:</strong>
  <%= @word.word %>
</p>

<%= button_to 'Search', word_searches_path(@word) %>

<table>
  <% @word.searches.each do |search| %>
    <tr>
      <td><%= search.result %></td>
      <td><%= search.created_at.getlocal %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Edit', edit_word_path(@word) %> |
<%= link_to 'Back', words_path %>
